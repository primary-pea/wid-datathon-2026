# subsaharan_data/ — country context pulls

What goes here (Hope):

- `GDP_per_capita.csv` — World Bank `NY.GDP.PCAP.CD`; the series is in current US$ whatever the header says
- `food_insecurity.csv` — FAOSTAT FIES
- `subsahran_africa_countries.csv` — the 49-country list (the file name keeps its original spelling because the pipeline reads it by name)

How they were pulled and what was left out: `derived/DATA_ACQUISITION.md`. The pipeline reads this folder by name from the repo root.
