# scaffold/ — the pipeline

Lands here as one copy from the freeze (scaffold v2.0, 12 September 2026, data version `afe63e46c26b`): the numbered scripts `00_assemble_panel.py` … `09_dml_sidebar.py`, `gifna_direct.py`, `config.py`, `util.py`, `figstyle.py`, `run_all.sh`, `tests/`, `inputs/` with a `*.provenance.json` beside every file, `results/` (RESULTS.md, the result CSVs, `stats/*.json`, `figures/*.png`) and the executed cross-check notebook. Kept by Saarah — nothing to push here.
